$color-danger = #f43f3f;
$color-info = #0077c7;
$color-success = #00c78d;
$color-warning = #f09000;

borderAndShadow(color, bgColor = #fff) {
  border-color: color;
  background: bgColor;
  box-shadow: inset 0 0 4px rgba(color, 0.075), 0 0 6px rgba(color, 0.6);

  .alert-title, .alert-close {
    color: color;
  }
}

.nk-alert {
  margin-bottom: 0;
  padding: 16px 8px;
  line-height: 16px;

  .alert-title {
    font-size: 16px;
    display: block;

    >span {
      font-weight: bold;
      font-size: 14px;
      line-height: 16px;
    }
  }

  .alert-close {
    opacity: 1;
    line-height: 16px;
  }

  &:not(.nk-alert-inline) {
    .alert-title {
      margin-bottom: 8px;
    }
  }

  &.nk-alert-inline {
    .alert-title {
      display: inline;
    }
  }

  &.nk-alert-danger {
    borderAndShadow($color-danger, #fef6f7);
  }

  &.nk-alert-info {
    borderAndShadow($color-info, #f5f9fd);
  }

  &.nk-alert-success {
    borderAndShadow($color-success, #f7fbf3);
  }

  &.nk-alert-warning {
    borderAndShadow($color-warning, #fffaf2);
  }
}
